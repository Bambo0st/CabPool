pipeline {
    agent any

    stages {
        stage('Git Clone') {
            steps {
                script {
                    sh 'git clone https://github.com/username/repository.git'
                }
            }
        }
    }
}
